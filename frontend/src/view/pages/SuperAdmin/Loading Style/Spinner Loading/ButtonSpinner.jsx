// ButtonSpinner.jsx
import React from 'react';
import './ButtonSpinner.css'; // Import the CSS for the spinner

const ButtonSpinner = () => {
  return (
    <div className="button-spinner"></div>
  );
};

export default ButtonSpinner;
